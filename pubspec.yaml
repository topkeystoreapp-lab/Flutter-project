name: hello_world_flutter
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  # Match Flutter stable on CI (subosito/flutter-action channel: stable).
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true